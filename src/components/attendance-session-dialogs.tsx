import { useEffect, useState } from "react"
import { DownloadIcon, QrCodeIcon } from "lucide-react"
import { toast } from "sonner"
import { mockAcademicYears } from "@/mock/academic-years"
import { mockSections } from "@/mock/sections"
import type { TeacherAttendanceSession } from "@/types/teacher-attendance"
import { Button } from "@/components/ui/button"
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"

const assignedSections = mockSections.filter((section) => section.teacherId === "teacher-1")
type SessionInput = Omit<TeacherAttendanceSession, "id" | "qrValue">
export function CreateAttendanceSessionDialog({ open, onOpenChange, onCreate }: { open: boolean; onOpenChange: (open: boolean) => void; onCreate: (input: SessionInput) => TeacherAttendanceSession }) {
  const [form, setForm] = useState<SessionInput>({ sectionId: assignedSections[0]?.id ?? "", academicYearId: assignedSections[0]?.academicYearId ?? "", title: "", date: "2026-07-26", attendanceType: "Time In", startTime: "08:00", endTime: "09:00", status: "Upcoming" })
  useEffect(() => { if (!open) return; setForm({ sectionId: assignedSections[0]?.id ?? "", academicYearId: assignedSections[0]?.academicYearId ?? "", title: "", date: "2026-07-26", attendanceType: "Time In", startTime: "08:00", endTime: "09:00", status: "Upcoming" }) }, [open])
  const set = <K extends keyof SessionInput>(key: K, value: SessionInput[K]) => setForm((current) => ({ ...current, [key]: value }))
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="sm:max-w-xl"><DialogHeader><DialogTitle>Create attendance session</DialogTitle><DialogDescription>A unique mock session ID and QR code are generated when saved.</DialogDescription></DialogHeader><form className="grid gap-4" onSubmit={(event) => { event.preventDefault(); onCreate(form); onOpenChange(false) }}><div className="grid grid-cols-2 gap-3"><div className="grid gap-1.5"><Label>Section</Label><Select value={form.sectionId} onValueChange={(value) => { const section = assignedSections.find((item) => item.id === value); setForm((current) => ({ ...current, sectionId: value ?? "", academicYearId: section?.academicYearId ?? current.academicYearId })) }}><SelectTrigger className="w-full"><SelectValue /></SelectTrigger><SelectContent>{assignedSections.map((section) => <SelectItem key={section.id} value={section.id}>{section.code}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-1.5"><Label>Academic school year</Label><Select value={form.academicYearId} onValueChange={(value) => set("academicYearId", value ?? "")}><SelectTrigger className="w-full"><SelectValue /></SelectTrigger><SelectContent>{mockAcademicYears.map((year) => <SelectItem key={year.id} value={year.id}>{year.name}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-1.5"><Label htmlFor="session-title">Session title</Label><Input id="session-title" value={form.title} onChange={(event) => set("title", event.target.value)} placeholder="e.g. Morning attendance" required /></div><div className="grid grid-cols-2 gap-3"><div className="grid gap-1.5"><Label htmlFor="session-date">Date</Label><Input id="session-date" type="date" value={form.date} onChange={(event) => set("date", event.target.value)} required /></div><div className="grid gap-1.5"><Label>Attendance type</Label><Select value={form.attendanceType} onValueChange={(value) => set("attendanceType", (value ?? "Time In") as SessionInput["attendanceType"])}><SelectTrigger className="w-full"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="Time In">Time In</SelectItem><SelectItem value="Time Out">Time Out</SelectItem><SelectItem value="Both Time In & Time Out">Both Time In & Time Out</SelectItem></SelectContent></Select></div></div><div className="grid grid-cols-3 gap-3"><div className="grid gap-1.5"><Label htmlFor="start-time">Start time</Label><Input id="start-time" type="time" value={form.startTime} onChange={(event) => set("startTime", event.target.value)} required /></div><div className="grid gap-1.5"><Label htmlFor="end-time">End time</Label><Input id="end-time" type="time" value={form.endTime} onChange={(event) => set("endTime", event.target.value)} required /></div><div className="grid gap-1.5"><Label>Status</Label><Select value={form.status} onValueChange={(value) => set("status", (value ?? "Upcoming") as SessionInput["status"])}><SelectTrigger className="w-full"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="Upcoming">Upcoming</SelectItem><SelectItem value="Active">Active</SelectItem><SelectItem value="Ended">Ended</SelectItem></SelectContent></Select></div></div><DialogFooter><Button type="button" variant="outline" onClick={() => onOpenChange(false)}>Cancel</Button><Button type="submit" disabled={!form.sectionId}><QrCodeIcon /> Create session</Button></DialogFooter></form></DialogContent></Dialog>
}

export function SessionQrDialog({ session, open, onOpenChange }: { session?: TeacherAttendanceSession; open: boolean; onOpenChange: (open: boolean) => void }) { const section = mockSections.find((item) => item.id === session?.sectionId); const cells = Array.from({ length: 121 }, (_, index) => ((index * 17 + (session?.id.length ?? 0) * 11) % 7) < 3); return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="sm:max-w-md"><DialogHeader><DialogTitle>{session?.title}</DialogTitle><DialogDescription>{section?.code} · {session?.date}</DialogDescription></DialogHeader>{session && <div className="space-y-4"><div className="mx-auto grid w-52 grid-cols-11 gap-0.5 rounded-xl bg-white p-3 shadow-sm ring-1 ring-border">{cells.map((filled, index) => <span key={index} className={`aspect-square ${filled ? "bg-slate-950" : "bg-white"}`} />)}</div><dl className="grid grid-cols-2 gap-3 rounded-lg bg-muted/50 p-3 text-sm"><div><dt className="text-muted-foreground">Attendance type</dt><dd className="font-medium">{session.attendanceType}</dd></div><div><dt className="text-muted-foreground">Status</dt><dd className="font-medium">{session.status}</dd></div><div><dt className="text-muted-foreground">Start / end</dt><dd className="font-medium">{session.startTime} – {session.endTime}</dd></div><div><dt className="text-muted-foreground">Session ID</dt><dd className="font-mono text-xs">{session.id}</dd></div></dl><Button className="w-full" variant="outline" onClick={() => toast.success("Mock QR code download started")}><DownloadIcon /> Download QR code</Button></div>}</DialogContent></Dialog> }
